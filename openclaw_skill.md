# HBT Skill for OpenClaw

## Описание

HBT (Hierarchical Block Text) — инструмент для управления иерархическими задачами проекта. Используй его для:
- Понимания структуры проекта
- Получения следующей задачи
- Отметки прогресса выполнения
- Декомпозиции сложных задач

## Основные команды

### Просмотр структуры проекта

```bash
hbt view
```

Показывает дерево задач с иконками:
- ⚪ todo — не начато
- 🔵 doing — в работе
- 🟢 done — выполнено
- 🔴 blocked — заблокировано
- 🔒 locked — защищено от изменений
- ✍️ editable — можно редактировать

### Получение следующей задачи

```bash
hbt next
```

**ВАЖНО**: Всегда используй эту команду, чтобы понять, что делать дальше. Команда автоматически находит следующую невыполненную задачу.

### Отметка начала работы

```bash
hbt set-progress --id <ID> --state doing
```

Выполни эту команду ПЕРЕД началом работы над задачей.

### Отметка завершения

```bash
hbt set-progress --id <ID> --state done
```

Выполни эту команду ПОСЛЕ успешного завершения задачи.

### Отметка блокировки

```bash
hbt set-progress --id <ID> --state blocked
```

Используй, если задача заблокирована внешними факторами (ждём API, нужен доступ и т.д.).

### Добавление подзадачи

```bash
hbt add --to <PARENT_ID> --text "Описание подзадачи"
```

Используй для декомпозиции сложных задач на более мелкие.

### Поиск задачи

```bash
hbt search "ключевое слово"
```

### Просмотр пути до задачи

```bash
hbt path --id <ID>
```

## Workflow

### Стандартный цикл работы

1. **Получи контекст проекта:**
   ```bash
   hbt view
   ```

2. **Найди следующую задачу:**
   ```bash
   hbt next
   ```

3. **Отметь начало работы:**
   ```bash
   hbt set-progress --id <ID> --state doing
   ```

4. **Выполни задачу** (напиши код, создай файлы и т.д.)

5. **Отметь завершение:**
   ```bash
   hbt set-progress --id <ID> --state done
   ```

6. **Повтори с шага 2**

### Декомпозиция сложной задачи

Если задача слишком большая:

1. **Разбей на подзадачи:**
   ```bash
   hbt add --to <TASK_ID> --text "Подзадача 1"
   hbt add --to <TASK_ID> --text "Подзадача 2"
   hbt add --to <TASK_ID> --text "Подзадача 3"
   ```

2. **Или используй rewrite для массового добавления:**
   ```bash
   hbt rewrite --id <TASK_ID> "Подзадача 1" "Подзадача 2" "Подзадача 3"
   ```

3. **Получи первую подзадачу:**
   ```bash
   hbt next
   ```

## Правила работы

### ✅ ДЕЛАЙ

- Всегда начинай с `hbt view` для понимания контекста
- Используй `hbt next` для получения следующей задачи
- Отмечай `doing` перед началом работы
- Отмечай `done` после завершения
- Декомпозируй большие задачи на мелкие

### ❌ НЕ ДЕЛАЙ

- Не пытайся редактировать узлы со статусом 🔒 (locked)
- Не пропускай отметку прогресса
- Не работай над несколькими задачами одновременно
- Не удаляй задачи без необходимости

## Идентификаторы

- **ID** — 12-символьный уникальный идентификатор (например: `a1b2c3d4e5f6`)
- **Alias** — короткое имя с префиксом @ (например: `@auth`, `@api`)
- В командах можно использовать как ID, так и alias

## Примеры

### Пример 1: Простая задача

```bash
$ hbt next
🎯 Следующая задача: a1b2c3d4 — Написать unit-тесты для auth модуля [todo]

$ hbt set-progress --id a1b2c3d4 --state doing
✅ Прогресс a1b2c3d4 -> doing

# ... пишем тесты ...

$ hbt set-progress --id a1b2c3d4 --state done
✅ Прогресс a1b2c3d4 -> done

$ hbt next
🎯 Следующая задача: b2c3d4e5 — Настроить CI/CD pipeline [todo]
```

### Пример 2: Декомпозиция

```bash
$ hbt next
🎯 Следующая задача: c3d4e5f6 — Реализовать систему авторизации [todo]

# Задача слишком большая, декомпозируем:
$ hbt add --to c3d4e5f6 --text "JWT токены"
✅ Добавлено: d4e5f6a7 [E]

$ hbt add --to c3d4e5f6 --text "Refresh токены"
✅ Добавлено: e5f6a7b8 [E]

$ hbt add --to c3d4e5f6 --text "OAuth 2.0"
✅ Добавлено: f6a7b8c9 [E]

$ hbt next
🎯 Следующая задача: d4e5f6a7 — JWT токены [todo]
```

### Пример 3: Работа с alias

```bash
$ hbt view --id @api
└── b2c3d4e5 @api ⚪ Backend API #backend ✍️
    ├── e5f6a7b8 @auth ⚪ Аутентификация #auth ✍️
    └── f6a7b8c9 @db ⚪ База данных #database ✍️

$ hbt set-progress --id @auth --state doing
✅ Прогресс e5f6a7b8 -> doing
```

## Статистика проекта

```bash
hbt stats
```

Показывает общую статистику: сколько задач выполнено, сколько осталось, топ тегов.

## Экспорт для отчёта

```bash
hbt export report.txt
```

Создаёт текстовый файл с деревом задач для отчёта или передачи другому агенту.

---

## Совместимость

Этот скилл совместим с:
- **OpenClaw** — автономный агент
- **Aider** — AI-ассистент для кода
- **Cline** — VS Code расширение
- **Cursor** — AI IDE
- **Continue** — open-source AI-ассистент
- **Любой агент** с поддержкой shell-команд

## Автор

HBT (Hierarchical Block Text) v1.0.0
Автор: Максим Кузьминский <i@m-letto.ru>
Лицензия: CC BY 4.0
